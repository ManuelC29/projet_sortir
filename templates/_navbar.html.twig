<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <img src="{{ asset('images/logo.png') }}" style="width: 50px;height: 50px;" >
            <a class="nav-item nav-link active" href="{{ path('welcome') }}">Sortir.com</a>
                {%if is_granted('IS_AUTHENTICATED_FULLY') %}
                <!--Si l'user est authentifié -->
                        <a href="{{ path('profilShow', {'id': participant.id}) }}">Mon profil</a>
                        <a class="nav-item nav-link active" href="{{ path('tripAdd') }}">Créer une Sortie</a>
                        <a class="nav-item nav-link active" href="{{ path('logout') }}">Se déconnecter</a>
                    <!-- TODO Ajouter un lien vers la page admin si on est admin -->

                {% else %}
                        <a class="nav-item nav-link active" href="{{ path('registration') }}">S'enregistrer</a>
                        <a class="nav-item nav-link active" href="{{ path('login') }}">Se connecter</a>
                {% endif %}
</nav>